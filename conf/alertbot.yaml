server:
  applicationConnectors:
    - type: http
      port: 8080

  requestLog:
  appenders: []

cryptoDir: /var/lib/alertbot/crypto  # path to dir where to store crypto box files - directory must exists!
apiKeys: []
